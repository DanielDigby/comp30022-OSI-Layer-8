import React from "react";

// Semantic UI button

import MenuItem from "./Menu";

const NotesView = (): JSX.Element => {
    // api call

    return (
        <div>
            <MenuItem />
        </div>
    );
};
export default NotesView;
